version: '3.8'

services:
  aggregator:
    build: .
    volumes:
      - .:/app/data
    environment:
      - INPUT_FILE=/app/data/2026.06.26.txt
      - OUTPUT_FILE=/app/data/chart.png
      - N_SECONDS=60
      - CANDLE_WIDTH_PX=15

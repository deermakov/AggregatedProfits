version: '3.8'

services:
  aggregator:
    build: .
    volumes:
      - .:/data
      - .:/output
    environment:
      - INPUT_FILE=/data/2026.06.26.txt
      - OUTPUT_FILE=/output/chart.png
      - INTERVAL_N=300

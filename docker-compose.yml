version: '3.8'

services:
  aggregator:
    build: .
    volumes:
      - .:/app/data
    environment:
      - INPUT_FILE=/app/data/2026.06.26.txt
      - OUTPUT_IMAGE=/app/data/result_chart.png
      - TIME_STEP_SEC=10
      - PRICE_STEP=10
      - PERCENTILE_GRID_SIZE=10
